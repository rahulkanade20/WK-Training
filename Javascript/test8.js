// creating web server using flask and calling it in javascript using async await or fetch etc.

// https://dev.to/matheusguimaraes/fast-way-to-enable-cors-in-flask-servers-42p0

// 0.0.0.0 address ?

// In the Internet Protocol Version 4, the address 0.0.0.0 is a non-routable meta-address 
//used to designate an invalid, unknown or non-applicable target. 
//This address is assigned specific meanings in a number of contexts, such as on clients or on servers.

// A 0.0.0.0 address indicates the client isn't connected to a TCP/IP network, 
// and a device may give itself a 0.0. 0.0 address when it is offline.

// cross origin resource sharing (CORS)

// cross site request forgery

// access control allow methods

// accept control allow origin

// same origin policy

// simple requests , preflight requests

// A 0.0.0.0 address indicates the client isn't connected to a TCP/IP network, 
// and a device may give itself a 0.0. 0.0 address when it is offline.

// https://www.hello.org:8080
// scheme:domain:port

// sending messages to teams or writing data to a file directly using flask app









